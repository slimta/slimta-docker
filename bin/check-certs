#!/bin/bash

exec docker run --rm \
        --volume '/etc/ssl/private:/etc/ssl/private' \
        --env-file /opt/slimta-docker/etc/host.env \
        --env-file /opt/slimta-docker/etc/lexicon.env \
        letsencrypt
